<div class="wrapper">
  <%= render "shared/side-bar" %>
    <div class="main_content">
      <h1>CALENDAR</h1>

      <%# <script src='https://cdn.jsdelivr.net/npm/fullcalendar@6.1.8/index.global.min.js'></script>
      <div id="calendar" data-controller="calendar" data-calendar-target="calendar">
        <div id='draggable-el' data-controller='draggable' data-event='{ "title": "my event", "duration": "02:00" }'>drag me</div> %>
      <%= month_calendar(events: @bookings, end_attribute: :end_time) do |date, bookings| %>
        <%= date %>

        <% bookings.each do |booking| %>
          <div class="meeting">
            <p><%= booking.agency %></p>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
